# actionlint configuration.
# https://github.com/rhysd/actionlint/blob/main/docs/config.md

self-hosted-runner:
  labels: []

# Reusable workflows are consumed by many repositories, each of which defines its
# own environment variables. Listing them here would mean editing this repository
# every time a consumer adds one, so name checking for `vars.*` stays off.
config-variables: null
